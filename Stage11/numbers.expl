int main(){

decl 
	int temp, num, maxnum;
enddecl

begin

	num = 1;
	temp = exposcall("Read", -1, maxnum);
	while(num<=50) do
		temp = exposcall("Write", -2, num);
		num = num + 1;

	endwhile;
	return 0;
end
}
