decl
  int a;
enddecl

int main(){
decl
  int temp, fileDesc, rootDesc;
  str uname, word, fileName;
enddecl
begin
  temp = exposcall("Write", -2, "Enter username");
  temp = exposcall("Read", -1, uname);
 
  temp = exposcall("Remusr", uname);
  if(temp == -1) then
    temp = exposcall("Write", -2, "User dne");
  else

  if(temp == -2) then
    temp = exposcall("Write", -2, "Perm deny");
  endif;
  endif;
  

	return 0;
end
}
